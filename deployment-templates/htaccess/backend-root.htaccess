<IfModule mod_rewrite.c>
    RewriteEngine On
    # Allow access to public directory only
    RewriteRule ^public(/.*)?$ - [L]
    # Block direct access to all other backend files
    RewriteRule ^ - [F,L]
</IfModule>

Options -Indexes
